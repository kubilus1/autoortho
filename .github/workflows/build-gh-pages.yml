# Sample workflow for building and deploying a Jekyll site to GitHub Pages
name: Deploy Jekyll with GitHub Pages dependencies preinstalled

on:
  # Runs on pushes targeting the default branch
  push:
    branches: ["main"]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

  pull_request:

# Sets permissions of the GITHUB_TOKEN to allow deployment to GitHub Pages
permissions:
  contents: read
  pages: write
  id-token: write
  pull-requests: write

# Allow one concurrent deployment
concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  # Setup

  # Build job
  build:
    runs-on: ubuntu-latest
#container:
#image: squidfunk/mkdocs-material:latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Setup
        run: |
          python3 -m pip install mkdocs-material mike
      - name: Configure
        run: |
          git config --global user.name Doc Deploy
          git config --global user.email docs@email.com
      - name: Publish
        run: |
          mike deploy --push --update-aliases unstable latest
      #- name: Build docs
      #  run: |
      #    docker run --rm -v `pwd`:/docs squidfunk/mkdocs-material build -d ./_site
      #- name: Upload artifact
      #  uses: actions/upload-pages-artifact@v1

  # Deployment job
  # deploy:
  #   environment:
  #     name: github-pages
  #     url: ${{ steps.deployment.outputs.page_url }}
  #   runs-on: ubuntu-latest
  #   needs: build
  #   steps:
  #     - name: Deploy to GitHub Pages
  #       id: deployment
  #       uses: actions/deploy-pages@v1
